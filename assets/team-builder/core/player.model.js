(function() {
'use strict';

    angular
        .module('team-builder')
        .factory('Player', Player);

    // Player.$inject = [''];
    function Player() {
        var Player = function () {
        };
        
        return new Player;

        ////////////////
    }
})();